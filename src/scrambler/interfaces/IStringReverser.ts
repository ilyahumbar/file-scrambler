export interface IStringReverser {
    /**
     * Reverses string given.
     * @param {string} str The string to reverse.
     * @returns {string} reversed string
     */
    reverse(str: string): string;
}